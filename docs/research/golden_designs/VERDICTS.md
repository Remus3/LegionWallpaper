## weapon: Weapon Fix Pass - Canonical Wrist-Mounted Repeating Crossbow (lw-gen golden blocker #1)
RECOMMENDATION: Ship the weapon fix as a 4th sidecar stage (tools/lw_gen_weaponfix.py) built on the free asset the pipeline already computes and discards: OpenPose wrist/elbow/hand keypoints, which localize the weapon region with no detection model. Escalate cheapest-first: W1 keypoint-masked inpaint re-roll (zero downloads, from_pipe reuses loaded weights), then W2 reference-transplant plus guided inpaint (affine-fit a real official-skin crossbow crop to the wrist, harmonize at strength 0.35-0.5) as the expected workhorse - the img2img face-blur rejection does not apply because the mask excludes the face and a hard paste-back keeps out-of-mask pixels bit-identical. Escalate to W3 IP-Adapter (supported on the SDXL inpaint pipe in diffusers 0.39) and W4 a pass-scoped weapon-concept LoRA only on measured shortfall. Acceptance is a calibrated weapon-region CLIP gate (crossbow positives vs blade/bow/rifle distractors, floors set by scoring official-skin crops against the 21 known-bad candidates) plus region sharpness and a full-image identity non-regression; the operator blesses the first golden exemplars. Milestone 1 (keypoints, mask spec, W1, gate calibration) is one session and de-risks everything downstream.
VERDICT [BOX-CONSTRAINT skeptic - every proposed model/API/dep verified against installed source in .venv-gen; SETTLED-list compliance audit]: sound_with_fixes
  FIX: FROM_PIPE CLAIM vs INTERLOCK CONTRADICTION (sec 2 VRAM audit + sec 5 vs sec 7): weaponfix is a SEPARATE subprocess per the interlock contract (lw_gen_run.py:7-10, design sec 7), so there is no loaded base_pipe in-process to derive from - the 'zero extra VRAM / no reallocation' from_pipe claim only holds in-process. Fix: weaponfix loads StableDiffusionXLInpaintPipeline.from_single_file(checkpoint) directly (FromSingleFileMixin confirmed on the class, pipeline_stable_diffusion_xl_inpaint.py:220) and SKIPS loading the xinsir ControlNet weights entirely (~2.5GB saved vs the gen process). The from_pipe(base_pipe, controlnet=None) path itself IS valid in 0.39 (auto_pipeline.py:1174-1188 verified; leftover controlnet=None kwarg is harmlessly ignored at 1209) but only applies if the pass ever moves in-process.
  FIX: CHECKPOINT SOURCE: tools/lw_gen_config.json:2 model_path STILL points at retired RealVisXL_V5.0; Animagine XL 4.0 (on disk at tools/models/animagine-xl-4.0) is reached only via the --model-path CLI override (lw_gen_run.py:522-524, 696-697), which IS recorded into the manifest 'model' field (lw_gen_run.py:329). weaponfix MUST take the checkpoint from manifest['model'], never from config model_path, or it harmonizes weapon regions with the retired RealVis base - an accidental settled-list violation. (Separately: updating config model_path to Animagine is an open engine chore.)
  FIX: OFFLOAD + IP-ADAPTER ORDER: call enable_model_cpu_offload() on the inpaint pipe explicitly, and (re-)call it AFTER load_ip_adapter - the loader does image_encoder.to(self.device) at load time (ip_adapter.py:214) and register_modules, so an encoder loaded after offload was enabled sits outside the hook chain (VRAM leak of ~1.3GB bf16 ViT-H). enable_model_cpu_offload safely rebuilds the chain (remove_all_hooks first, pipeline_utils.py:1223). Same rule after load_lora_weights/unload_lora_weights in W4 is not needed (LoRA lives inside the unet), but the IP-Adapter case is.
  FIX: HAND-KEYPOINT SENTINELS: controlnet_aux encodes MISSING hand peaks as -1 normalized coords (open_pose/__init__.py:126-127), not None - the mask spec term 'bbox of the 21 hand kps' (sec 4 item c) must filter kp.x < 0 or kp.y < 0 first, or a single missing peak drags the bbox to the image origin and the mask blows past the 35% area cap or swallows the face disc.
  FIX: DETECT RESOLUTION: OpenposeDetector.__call__ resizes to detect_resolution=512 before detection (open_pose/__init__.py:199 signature; gen-time skeletons were extracted through that path), but the design calls detect_poses() RAW on the 1344x768 candidate - body estimation then runs at native res, a different scale regime than anything validated. Resize the candidate to ~512-768 short side before detect_poses; returned coords are normalized (open_pose/__init__.py:186-192) so all downstream mask math is unchanged.
VERDICT [EFFICACY skeptic - will each mechanism actually fix the named failure, and how would we know per iteration]: sound_with_fixes
  FIX: OLD-WEAPON COVERAGE GAP (must fix before M1 exit): the mask spec localizes where the canonical weapon SHOULD be, not where the wrong weapon IS. In 5/5 reference images the non-canonical prop extends far outside the spec union (seed22: blade runs to the lower-right frame edge; seed33: axe head sits ~3-4 forearm-lengths beyond the fist; seed800: blades in BOTH hands; cand_01: glaive spans wrist to top-right corner; cand_02: spear crosses the whole left half plus a shouldered prop by the head). Inpainting only disc(W,0.9L)+disc(W+1.1v,1.2L)+hand-bbox leaves old-prop remnants -> crossbow PLUS floating blade = still fails the operator weapon criterion. Required: an explicit old-prop removal? - mandatory second-hand region (not 'optional later iteration'), an adaptive mask extension along the prop axis, or a cheap old-prop locator - PLUS an M1 measured acceptance check: coarse-box the wrong weapon on the 21 known-bads and require mask coverage >= ~95% of old-weapon pixels before any W2 effort.
  FIX: POST-FIX QA RE-SHELL SCORES THE WRONG FILE: lw_gen_qa.score_batch iterates candidates[] and scores cand['file'] (lw_gen_qa.py:327-332). Re-shelling QA after weaponfix re-scores the ORIGINAL cand_XX.png, not cand_XX_wfix.png, so the identity non-regression check as specified never sees the fixed image. Weaponfix must rewrite cand['file'] to the wfix name (or QA must learn a weapon{file} preference) before the re-shell claim is true.
  FIX: T_wblur=150 IS AN INVALID TRANSFER: 150 was calibrated on FULL 1344x768 images (GEN_RETUNE.md:78-81, lw_gen_config.json:34); laplacian variance does not transfer to small crops, and canonical weapons are legitimately DoF/motion-blurred (seed33's weapon sits in the blur plane; the official default splash's rig is dark and partly blurred). The 'immune to the DoF confound' claim is wrong when the weapon itself is in the blurred plane - official-skin POSITIVE crops may fail the very floor used to calibrate them. Calibrate T_wblur on crops during M1 or drop the region blur floor.
  FIX: OPENPOSE-ON-ANIME RELIABILITY IS UNPROVEN, NOT PROVEN: 'OpenPose already proved reliable on painted splash art' conflates the gen-time input (REAL semi-realistic Riot splashes) with the new use (COCO-photo-trained OpenPose run on Animagine anime-flat OUTPUT). Add an M1 hard exit criterion: measured wrist/hand detection recall on the 21 known-bad candidates (e.g. >= 80 percent usable RWrist+RElbow), else the whole localization premise fails. Also: (a) mirror the proven preprocessing - __call__ resizes to detect_resolution=512 before detect_poses (controlnet_aux __init__.py:216-220); calling detect_poses raw at 1344x768 is an untested path; (b) handle zero-body and multi-body PoseResult lists (pick max total_score); (c) note left/right chirality flips on profile/back poses put the mask on the wrong arm - the fallback chain does not catch a CONFIDENT wrong-side detection.
  FIX: BATCH-SKELETON FALLBACK CONTRADICTS THE RECIPE'S OWN FINDING: at cn 0.55 the model 'composes freely' and candidates drift from the batch skeleton (GEN_RETUNE.md:94-100, design sec 4 admits this). Fallback 2 therefore places masks off-target with no error signal; it should demote to the review lane, not silently proceed to inpaint.
  FIX: GATE SEPARATION NEEDS A NUMERIC ABORT CRITERION: precedent says CLIP text-softmax gates can be DEAD (T_aes scores everything 0.500-0.504, lw_gen_config.json:37), and fine-grained object discrimination on small dark crops is CLIP's weakest regime ('wrist-mounted crossbow' vs 'hand-held crossbow' vs 'a blurry dark shape'). The design says 'never ship an uncalibrated gate' but sets no separation target. Specify one (e.g. zero overlap between official-crop min and known-bad max, or a margin AUC floor) and a defined fallback (operator-only lane) if CLIP cannot separate.
  FIX: QUANTIFY PER-RUNG ACCEPTANCE AND THE ESCALATION TRIGGER: 'acceptance stays under target' is undefined. Set numbers per rung (e.g. W1 salvage >= 20 percent of K-roll candidates on the known-bad set; W2 >= 50 percent operator-pass on the 3 accepted references re-processed; M2 exit is currently n=1 operator bless - too weak to validate a workhorse). All P(canonical) column values (0.6-0.8 for A, etc.) are assertions with zero pilot data - label them plausible-unproven and let M1/M2 measurements replace them.
  FIX: FACE-EXCLUSION RULE DISABLES THE FIX ON A KNOWN CASE: cand_02's shouldered prop sits beside the head; its ROI intersects the face disc, so the design's own hard rule routes it straight to review - i.e. one of the 5 reference candidates is unfixable as specified. Acceptable, but state it and count it against expected salvage rates.
  FIX: HAND DEGRADATION RISK INSIDE THE MASK: the mask deliberately includes the hand bbox and W1 re-rolls it at strength 0.92 with the same UNet whose #2 failure mode is hands (GOLDEN_DEFINITION.md:43). A weapon PASS can ship a newly mangled hand - the weapon gate does not look at the hand and full-image subject_cos will not catch it. Add at least an operator-lane note or a hand-region sanity check to the accept path.
  FIX: 'ZERO EXTRA VRAM / FREE RUNG' FRAMING IS WRONG FOR THE ACTUAL ARCHITECTURE: sec 5's from_pipe derivation assumes the base pipe is resident, but sec 7 makes weaponfix a SEPARATE 4th-stage process - it pays a full SDXL load (and its own enable_model_cpu_offload; hooks from the parent process do not transfer). Correct the cost model: W1 is cheap in downloads, not in wall-clock; also load a plain inpaint pipe directly in weaponfix instead of the from_pipe dance.
  FIX: W2 STYLE/GEOMETRY MISMATCH IS THE UNMEASURED CORE RISK: official crops are painterly semi-realistic; candidates are Animagine anime-flat - at strength 0.35-0.5 the paste can read as a sticker, and the affine fit infers 3D orientation from a 2D forearm projection (moderate foreshortening halves L without tripping the L<20px guard, yielding a half-scale rig; view choice from sign(v_hat.x) is crude). The rig in vayne_00_default is verified small/dark/partly occluded - 'clearest rigs' in the named skins is asserted, not checked. M2 needs a fast fail-early pilot (1 crop, 1 candidate, 3 strengths) before building the full asset library.
  FIX: W4 LORA DILUTION CAVEAT: 30-60 crops drawn from 19 heterogeneous skins is ~2-3 per skin of DIFFERENT rig designs - risk of learning 'generic dark crossbow', the same dilution family as the prior LoRA failure (GEN_RETUNE.md:128-130). If W4 triggers, train on default-rig crops (+augmentation) only.

## face_hands: lw-gen Face/Eyes + Glasses + Hands Verify-then-Repair Pass Design
RECOMMENDATION: Build the repair pass as a new stateless tools/lw_gen_repair.py that runs OpenposeDetector.detect_poses directly on each generated candidate - the keypoints the engine currently throws away (lw_gen_run.py:406-413) give free, normalized face/eye/wrist localization with zero new models, and controlnet_aux itself localizes hands from wrist geometry, validating the approach. Ship it verify-first: only two automated triggers are trustworthy today (face-crop lap_var and a glasses 3AFC round-glasses-vs-visor-vs-none CLIP check, both calibratable on the already-labeled exp3/exp4 corpus), while hands stay operator-flag-only because every automatic hand signal is unreliable on painted art. Order the pipeline weapon -> hands -> face -> glasses -> eyes, since the weapon inpaint overwrites hand regions and often fixes hands for free, and the face/glasses/eyes boxes are strictly nested. Never inpaint a chirality error or a missing-wrist hand - those are structural and only a fresh seed fixes them; cap repairs at 2 attempts per element / 5 per candidate and re-run the global identity gate on every repaired file. The face-crop sharpness metric built here should then be wired into lw_gen_qa as the deferred subject-region T_blur fix, killing the DoF false-reject (seed22) in the same effort.
VERDICT [BOX-CONSTRAINT skeptic - every model/API/dep verified live against .venv-gen/.venv-metrics site-packages and installed versions; SETTLED-list conflict scan]: sound_with_fixes
  FIX: from_pipe controlnet trap: lw_gen_repair.py MUST load a plain StableDiffusionXLPipeline.from_single_file (no controlnet) before AutoPipelineForInpainting.from_pipe. from_pipe on a controlnet-loaded pipe maps to StableDiffusionXLControlNetInpaintPipeline (.venv-gen diffusers auto_pipeline.py:245 vs 252), which requires a control_image every call. The gen path loads a CN pipe (lw_gen_run.py:370-372), so 'base_pipe' is ambiguous as written - state the plain load explicitly.
  FIX: Hand keypoint 'score floor' does not exist in this controlnet_aux version: hand/face Keypoints are built with x,y only so score is always the 1.0 default (open_pose/body.py:13-17, __init__.py:129-132,153-156); MISSING peaks are encoded as coordinate -1 (__init__.py:126-127,151-152) and body kps as None (line 190). The '>= 12 kps above score floor' / '>= 15/21 kps' checks must count keypoints with x >= 0 (non-sentinel), not score.
  FIX: detect_poses input contract: it takes a numpy uint8 RGB array (it BGR-flips internally at __init__.py:171), NOT a PIL image; and __call__ preprocesses with HWC3 + resize_image(detect_resolution=512) before detecting (lines 216-217). lw_gen_repair should replicate that preprocessing (np.array(pil), HWC3, resize_image 512) so candidate-side detection recall matches the gen-time skeleton behavior; raw 1344x768 input is geometrically valid (normalized coords) but is an unvalidated detection scale.
  FIX: Section 7 side-benefit (wiring face-crop lap_var into lw_gen_qa) must stay a SEPARATE follow-up with its own calibration + Tier-2 suite - it modifies the shipped QA gate whose floor calibration is settled (LEDGER 16); bundling it silently into the repair slice risks re-litigating T_blur without the discipline.
VERDICT [EFFICACY skeptic - will each proposed mechanism actually fix the named failure, or only plausibly?]: sound_with_fixes
  FIX: PRE-BUILD GATE (blocks all other work): measure candidate-side detect_poses reliability on the 6 labeled images (seed22/33/800, cand_01/02, plus one exp4 reject) BEFORE writing inpaint code. Overlay computed FACE/EYES/GLASSES/HAND boxes on a contact sheet; operator confirms boxes land on the element in >= 5/6. The openpose face estimator (70 kps) and hand estimator are photo-trained; on anime-flat faces behind red lenses (cand_02 visor, seed33 flat shading) detection may fail or misplace, and the whole localization premise rests on it. If face kps miss on most candidates, the FACE/EYES/GLASSES boxes fall back to body eyes 14/15, which brings the next fix into play.
  FIX: SCALE UNIT S IS WRONG FOR 3/4 VIEWS (all 5 viewed candidates are 3/4 or profile): dist(eye_l,eye_r) foreshortens with head yaw, shrinking the FACE box (4.2*S) below the actual face. Replace fallback-only logic with S = max(inter_eye, 0.6*dist(nose,neck)) always, and add a unit test with synthetic profile keypoints.
  FIX: SPEC BUG: hand keypoints carry NO score - open_pose/__init__.py:129-132 constructs Keypoint(x=peak[0], y=peak[1]) only; missing peaks are encoded as -1 coords (lines 126-127). The '>= 12 kps above score floor' check is unimplementable; respec as count of kps with x >= 0 and y >= 0.
  FIX: SPEC GAP: detect_poses requires a numpy array (open_pose/__init__.py:171 does oriImg[:,:,::-1]) and __call__ preprocesses with HWC3 + resize_image(img, 512) at lines 216-218 before detecting. Calling detect_poses(candidate_pil) crashes; calling it on raw 1344x768 numpy runs the body model at an untested scale. Pin the preprocessing to mirror __call__ (resize to detect_resolution 512, kps are normalized so they map back), and include the 512-vs-768 comparison in the pre-build gate.
  FIX: NORMALIZE CROP RESOLUTION BEFORE lap_var: laplacian variance is strongly resolution-dependent, and the FACE box side (4.2*S) varies 180-280 px across candidates. A floor calibrated on mixed-size crops is not comparable between candidates (this also poisons the section-7 side benefit of wiring it into lw_gen_qa). Resize every face crop to a fixed size (e.g. 256x256) before scoring, both at calibration and at runtime.
  FIX: RE-VERIFY IS PARTLY VACUOUS - CLOSE THE METRIC-GAMING HOLE: a repaired crop is generated at 1024 then Lanczos-DOWNSCALED to ~200 px for paste-back; downscaling concentrates high-frequency energy, so post-repair face-crop lap_var passes the sharpness floor almost unconditionally regardless of canon correctness. And the global Stage-A re-run (lw_gen_qa.py:106-111) uses whole-image CLIP cosine, which is insensitive to a ~200 px patch in a 1344x768 frame - it will not catch a botched face. Required: (a) an outside-mask no-change assertion (pixel diff, mirrors the cleaning-pass identity assertion) on every repair; (b) accepted repairs land in the operator contact sheet as before/after pairs with operator sign-off as the real acceptance, with lap_var demoted to a trigger, never an acceptance, for FACE/EYES.
  FIX: GLASSES 3AFC ONLY PLAUSIBLY CATCHES MODE (a): viewed evidence - seed22 and seed800 glasses are deformed/cat-eye but unambiguously GLASSES (modes b/c), so 'small round red-tinted glasses' vs 'red visor mask' vs 'no eyewear' will likely argmax glasses on them and never trigger repair; only cand_01/cand_02 are true visor-mask. The design's demote-if-no-separation clause is good but insufficient: the calibration report must score modes (a) and (b/c) as SEPARATE clusters and state per-mode recall, and expect the shipped auto-trigger to cover mode (a) only, with (b)/(c) operator-flagged. Do not claim the glasses failure class is auto-covered.
  FIX: VISOR-MASK REPAIR (strength 0.58) IS UNPROVEN AND ITS OWN SPEC UNDERMINES IT: a 0.72-interior mask leaves visor edges in the unmasked context ring, biasing the model to continue the red plate; the design switches to a FULL eye-band mask for mode (a), but then the eyes are re-invented from scratch with NO automated eye check ('re-verify EYES' has no auto metric - eyes are operator-flag). Acceptance check: before shipping, run the mode-(a) recipe on cand_02 and cand_01 specifically and require operator before/after approval; treat 'triggered repair also corrects shape (c)' as a hypothesis to be measured on seed22/seed800, not a stated property.
  FIX: FIX THE BUDGET ARITHMETIC: a 1024x1024 inpaint at 28 steps is ~1.05 MP of latent - the SAME cost as one full 1344x768 gen (1.03 MP), not 'far cheaper'; 5 attempts is ~5 full gens. Either correct the claim and revisit the repair-vs-reroll breakeven (a fresh seed costs the same as ONE repair attempt, which strengthens the give-up policy), or run repairs at 768/832 canvas where the cost claim becomes true.
  FIX: PER-ITERATION ACCEPTANCE CHECKS, WRITTEN INTO THE PLAN: iteration 0 = keypoint/box overlay gate (above); iteration 1 = calibration report (face lap_var clusters on fixed-size crops + glasses 3AFC per-mode clusters) checked into docs/research/ with explicit ship/demote decision per check, including a demotion clause for the FACE lap_var check (exp4 FAIL anchors seeds 2/5/8 are 'malformed' faces, not necessarily blurry ones - lap_var may not separate sharp-but-wrong from good, in which case FACE becomes operator-flag like hands); iteration 2 = single-image repair smoke tests (seed800 glasses, cand_02 visor, one soft face) with operator A/B; iteration 3 = full-batch rerun scored by a fresh operator critique, success defined as 'named element fixed with zero new failures introduced'. No iteration ships on visual plausibility alone.

## finish: lw-gen FINISH PASS design - element-corrected 1344x768 draw to golden 2560x1440 wallpaper
RECOMMENDATION: Crop each element-corrected candidate from 1344x768 to exactly-16:9 1344x756 (12 rows, 1.6 percent) inside lw-gen - this is mandatory, since Stage-1's aspect guard (ASPECT_TOL 0.02) refuses the raw 7:4 frame - and reject generating at 1536x864, which would re-open the shipped, calibrated recipe. Finish = both stages in sequence: a low-strength (0.30, cap 0.35) Animagine img2img detail refine at 2048x1152 in .venv-gen (adds the painted micro-texture a faithful upscaler cannot synthesize; ~40-70 s, <9 GB peak with the default offload), then the untouched proven Stage-1 chain post-intake (JaNai V3 DAT2 x4 to 8192x4608, one Lanczos to 2560x1440, light USM, full G1 gate) for supersampled crispness. Integrate as a new pre-promotion phase (tools/lw_gen_finish.py) running only on QA-PASS candidates, with promote and first-pass unchanged: 2048x1152 deliberately passes promote's SIZE ASSERT, keeps Stage 1 on the lap_ratio-valid upscale path (ADR-006 never triggers), and preserves the ADR-003 stop-at-0.Originals operator boundary. QA verdicts stay on the raw candidate (blur floors are 1344x768-calibrated); a Stage-A identity re-score guards against refine drift. Flagged follow-up: _extract_pose currently throws away OpenPose keypoint coordinates (lw_gen_run.py:406-413) - persist them in the manifest to give the upstream weapon/hand/face correction passes free region localization with no detection model.
VERDICT [BOX-CONSTRAINT skeptic - every model/API/dep vs diffusers 0.39 / torch 2.11 cu128 sm_120 / ~12GB / SDPA-only / pure-torch safetensors / venv separation, plus SETTLED-list re-litigation check]: sound_with_fixes
  FIX: MODEL-PATH TRAP: tools/lw_gen_config.json:2 model_path STILL points at the retired RealVisXL_V5.0; the winning Animagine recipe ran via the --model-path CLI override (GEN_RETUNE.md:32-33, override plumbing lw_gen_run.py:522-524). lw_gen_finish MUST take the base-model path from the batch gen_manifest (confirm build_manifest records the overridden config) or an explicit pin - reading config.model_path silently refines with RealVis, violating the Animagine-locked SETTLED line.
  FIX: FROM_PIPE CLASS TRAP: the cited pattern (lw_gen_run.py:602-606) lives in the NON-ControlNet branch, but the winning recipe loads StableDiffusionXLControlNetPipeline (lw_gen_run.py:592). AutoPipelineForImage2Image.from_pipe(controlnet_pipe) yields the ControlNet img2img variant that REQUIRES a control image at call time. Fix is one line - pass controlnet=None (verified in installed diffusers 0.39.0 source: from_pipe strips 'ControlNet' from the class name when controlnet=None) - or load the base fresh in the finish subprocess.
  FIX: F4 needs a new entry point: lw_gen_qa.py is batch-manifest driven (scores manifest candidates in place); a Stage-A-only single-file re-score of the 2048x1152 finish file does not exist today. Small .venv-metrics shim reusing ClipScorer (lw_gen_qa.py:209) - budget it, do not claim 'existing scorer, unchanged'.
VERDICT [EFFICACY skeptic - per-mechanism: will it actually fix the named failure, and how would we know?]: sound_with_fixes
  FIX: NO DEGRADATION GATE ON THE SHIPPED FILE: the refined 2048x1152 file is what ships, yet F4 explicitly skips T_blur on it and the raw-candidate QA verdict carries. G1 downstream cannot catch refine-induced softening either: lap_ratio compares the upscale to its own (already-soft) promoted input. The design's fallback claim 'worst case equals option (1)' is FALSE - a VAE encode/decode roundtrip plus few-step denoise (strength 0.30 x 30 steps = ~9 effective DPM++ 2M SDE steps, an untested regime) can strictly degrade fine detail below the raw candidate, so worst case is WORSE than option 1. Required: pre/post subject-region sharpness comparison per refine (the center-upper crop proxy already validated in scratchpad/pool_judge.py per GEN_RETUNE.md lines 116-123) with an automatic keep-raw fallback when the refined file loses high-frequency energy.
  FIX: STAGE-A TRIPWIRE CANNOT DETECT THE FAILURE IT GUARDS: the F4 cos-drop > 0.02 check is a global CLIP metric. Ground truth from the calibration data (GEN_RETUNE.md lines 62-74): good candidates span subject_cos 0.275-0.288 while off-canonical Heartseeker scores 0.228 - the entire good-vs-wrong-canon spread is ~0.05, and the exp4 volume batch with wrong weapons in ~15/16 images PASSED subject QA (the operator caught the weapons, not the gate). A refine that re-rolls the corrected wrist-crossbow into a blade will move global cos by far less than 0.02. Required: region-level pre/post checks (SSIM or crop-CLIP on wrist/hand/face boxes derived from the OpenPose keypoints the design itself proposes persisting in section 6) - or an explicit statement that element preservation rests solely on operator eyeballs at _firstneedauth, which is an unbudgeted per-image manual re-review.
  FIX: DETAIL SYNTHESIS IS PLAUSIBLE BUT UNPROVEN - DEMAND AN ITERATION-1 A/B ACCEPTANCE CHECK: the refine reuses the SAME model, SAME prompt (already carrying 'intricate details / sharp focus / absurdres'), and near-same conditioning that produced the smooth look in the first place; re-denoising at 1.52x with strength 0.30 adds high-frequency content (hires-fix folklore) but there is zero in-repo evidence the added content reads as painted micro-texture rather than more anime-flat smoothness. Also a target conflict: section 0 benchmarks against the official splash's dense PAINTERLY brushwork, but the settled recipe is deliberately ANIME-FLAT by operator override (GEN_RETUNE.md lines 29-38) - matching official brushwork may be the wrong goal. Required: iteration 1 = operator 2AFC of option-1 (raw + proven chain) vs option-3 (refine + proven chain) at 2560x1440 on seed22/seed33/seed800 BEFORE adopting the refine stage, plus a tracked (non-gating) subject-region high-frequency-energy delta per run so 'detail added' becomes measurable.
  FIX: PIPE CONSTRUCTION UNDER-SPECIFIED: F3 cites 'pattern already proven in-repo: tools/lw_gen_run.py:602-606', but that from_pipe call is the NON-ControlNet else-branch; the actual gen-time pipeline is a ControlNet pipeline (lw_gen_run.py:592). AutoPipelineForImage2Image.from_pipe on an SDXL ControlNet pipe in diffusers 0.39 yields a ControlNet img2img pipe that demands a control image - the 'NO ControlNet at this stage' intent needs either a fresh non-CN base load in the lw_gen_finish subprocess (re-budget ~model-load time into the 40-70s estimate) or explicit component stripping. Specify which, and prove it once on this box before claiming the pattern is proven.
  FIX: KEYPOINT/CROP COORDINATE CONTRACT: the crop shifts every y-coordinate by 6 px relative to the 1344x768 frame in which upstream element-correction masks and any persisted OpenPose keypoints are expressed. State the ordering + coordinate-space contract explicitly (all keypoint consumers operate pre-crop, or post-crop consumers apply the -6 y offset), or a future pass will localize regions 6 px off. Also downgrade 'no edge-critical content in the outer 6 rows' from a claim to a per-image check - it is an n=5 eyeball observation, and the official splash pose (mid-leap, boot near frame bottom) is exactly the kind of composition that would put content in the crop band.

## qa_fix: QA Gate Fix - Subject-Region Sharpness (DoF-Confounded lap_var) + Golden-Ranking Probes
RECOMMENDATION: Ship a dual sharpness metric: a fixed center-upper-crop laplacian variance (lap_var_subject, crop x 25-75 pct y 5-65 pct, pure numpy, zero new deps) becomes the Stage-B blur gate with its own floor T_blur_subject, while global lap_var drops to advisory; full-res review confirms the crop covers the subject in every accepted and near-miss candidate. Calibrate T_blur_subject offline from the already-graded exp3/exp4/tuned/proto corpus plus the proven synthetic Gaussian blur-sweep - no operator sweep needed - with the hard acceptance criterion that seed22/seed33/seed800/cand_01/cand_02 and all 6 tuned candidates pass while every blurred variant rejects. In the same slice, stop discarding OpenPose keypoints: add per-candidate detect_poses self-extraction in lw_gen_run (detector already resident, JSON handoff to the metrics venv) to persist subject/face bboxes and wrist points - advisory-only for QA today, but the exact localization the weapon/glasses/face repair passes and golden-ranking probes need. Gate on a bbox crop or any CLIP element-probe (weapon margin, glasses margin) only after each separates the operator's labeled exp4 critique set; the T_aes dead-gate is the cautionary precedent. Reject CLIP-based localization in the metrics venv outright. Tier-2 rollout with the failing-test-first list above, ending with a re-grade backfill that flips seed22's historical false-reject to PASS.
VERDICT [BOX-CONSTRAINT skeptic - every proposed API/dep/model checked against diffusers 0.39, no-xformers/insightface/mediapipe, sm_120 torch 2.11, ~12GB VRAM, pure-torch safetensors, venv separation, and the SETTLED list]: sound_with_fixes
  FIX: Sentinel handling is wrong as specced: BODY missing keypoints are None, not -1 (.venv-gen/Lib/site-packages/controlnet_aux/open_pose/__init__.py:190 'if keypoint is not None else None'), and hand/face missing keypoints are NOT -1.0 either - the -1 is divided by W/H (lines 126-127, 151-152: np.where(peak<1e-6, -1, peak+x)/float(W)), so a missing hand/face point lands at -1/1344 ~ -0.0007. _locate_subject and TDD test 11 must filter validity as 'keypoint is not None and x>=0 and y>=0', never '== -1'; a naive -1 check silently drags the bbox min toward 0/negative.
  FIX: Calling detector.detect_poses(np.asarray(image)) directly skips the __call__ preprocessing (HWC3 + resize_image to detect_resolution=512, __init__.py:216-217). Keypoint fractions stay valid (normalized by the input's own H,W at 188-189) and the RGB->BGR flip is internal (line 171, so RGB input is correct), but detection then runs multi-scale at raw 1344x768 on CPU (from_pretrained never calls .to(cuda); neither does _extract_pose, lw_gen_run.py:411) - materially slower than 'seconds' and a different detection regime than the 512px one the ref skeleton was extracted at. Replicate resize_image(HWC3(arr), 512) before detect_poses.
  FIX: The claim 'detector already resident in .venv-gen when the ControlNet path is active' is false as written: the detector is a local inside _extract_pose (lw_gen_run.py:409-413) and is garbage-collected on return; it also runs once per batch at :593 while _locate_subject runs per candidate at :468. The slice must explicitly change _extract_pose's return contract (return the detector and/or PoseResults) or hoist detector construction into run() - the design implies this but never states the contract change.
  FIX: Adding T_blur_subject to DEFAULT_THRESHOLDS breaks an existing test the design claims stays green: tests/test_lw_gen_qa.py:119 asserts set(resolved3) == {'T_subj','T_margin','T_aes','T_blur'} exactly. Update that assertion in the same slice. Likewise grade() must read T_blur_subject via thresholds.get(...) or only inside the lap_var_subject-is-not-None branch - existing tests pass a hand-built 4-key THRESH dict (test_lw_gen_qa.py:21) straight into grade() and an unconditional thresholds['T_blur_subject'] lookup KeyErrors them.
  FIX: Config drift to record while touching lw_gen_config.json: model_path still points at RealVisXL_V5.0 (lw_gen_config.json:2) even though Animagine is SETTLED-locked; batches evidently pass --model-path. Do not fix silently in this slice, but note it so the qa{} edit does not ship next to a stale-looking model note without comment.
VERDICT [EFFICACY skeptic - will each mechanism actually fix its named failure]: sound_with_fixes
  FIX: grade() must read T_blur_subject via .get() with fallback, and the TDD list must include updating existing tests: tests/test_lw_gen_qa.py:21 passes a hand-built 4-key THRESH dict into grade() (eager thresholds['T_blur_subject'] would KeyError ~8 tests) and tests/test_lw_gen_qa.py:119 asserts set(resolved) == exactly the 4 legacy keys, so adding T_blur_subject to DEFAULT_THRESHOLDS breaks it. The design's 'backward compatible by construction' claim is overstated until these are named.
  FIX: Detector lifetime is misdescribed: 'detector already resident' is false - the OpenposeDetector in _extract_pose is a local variable (tools/lw_gen_run.py:411) discarded on return, and _extract_pose runs at lw_gen_run.py:593 before _generate_candidates. The design must specify threading/retaining the detector instance into _generate_candidates and state that txt2img/img2img batches (lines 594-606) never construct one (nulls there are by-construction, not fallback).
  FIX: Per-candidate detect_poses must mirror __call__'s resize_image to detect_resolution ~512 (open_pose/__init__.py:216-217) and specify device: raw detect_poses on a full 1344x768 array with the detector on CPU (from_pretrained default; _extract_pose never calls .to) is untested for latency ('seconds' is asserted, not measured) and body-model accuracy is calibrated near 512. Keypoints are normalized to the passed array so resizing is coordinate-safe.
  FIX: _locate_subject and test 11 must handle BOTH missing-keypoint encodings: body keypoints are None entries (open_pose/__init__.py:190), hand/face use -1 coords (126-127, 151-152). A min/max that only filters -1 crashes or corrupts on None body keypoints. Test 11 as written ('-1 sentinels') misses the more common case.
  FIX: Calibration acceptance criteria are one-sided and near-vacuous on the reject side: the only required rejects are synthetic global Gaussian blurs, which necessarily crash the crop metric too (crop is a subset), so ANY floor in the huge window passes trivially. Add a measured datapoint on at least one natural soft/blotchy-face candidate, and if the metric cannot separate it, record the gate scope explicitly as 'catastrophic blur only' - do not let the slice imply it addresses the 'faces blotchy/blurry' critique (it does not; that lands on the unvalidated face_lap_var advisory).
  FIX: seed150 must NOT be used as blur-ordering ground truth in step 5: the critique line (GOLDEN_DEFINITION.md:30) says the HAND 'seems malformed but is blurry not sure' - it labels operator uncertainty about a hand, not image blur. With it removed, the ranking-validation set has ~zero labeled blur negatives, making the promised 'bbox promotes only if ordering at least as consistent' criterion vacuous. Require real blur negatives (degenerate low-step draws or fresh operator labels) before any bbox-gate promotion.
  FIX: Add a face-bbox coverage+correctness spot check (overlay bboxes on the exp3/exp4 corpus, count correct localizations) before face_lap_var is used even as a PASS-ranking tiebreaker: the OpenPose face estimator is photo-trained; on Animagine anime faces with large red glasses the likely failure is body-found-but-face-missing-or-misplaced, not the design's handled zero-pose case. A silently wrong advisory poisons golden ranking worse than a null.
  FIX: Weapon probe validation is underspecified: define per-image aggregation across two wrist crops (max? mean?), and handle the DoF-blurred-weapon case - seed33's accepted weapon is intentionally blurred and largely a featureless prop at CLIP resolution, so the 'seed33 must score weapon-plausible' criterion can fail for blur reasons and wrongly kill (or wrongly tune) the probe. Also note wrist-centered crops can miss the forearm-mounted rig and go degenerate at frame edges (cand_02 right arm exits frame).

## rubric: GOLDEN RUBRIC - Vayne Wallpaper Golden Definition v1
RECOMMENDATION: Ratify this rubric as-is and let it drive the three-pass ladder: weapon pass first (the forearm-mounted repeater has never rendered in any accepted candidate - it is the sole element at 0 across all five refs), then the keypoint-localized repair pass for glasses/hands/face, then the JaNai finish pass to 2560x1440. Before the first weapon pass, land the one small engine change the rubric depends on: persist the OpenPose wrist/hand/face keypoint coordinates that _extract_pose currently throws away (lw_gen_run.py:413, output_type="pil") - that single change gives free region localization for the weapon, repair, and the subject-region sharpness QA fix with zero new models. Adopt the pipe-delimited scorecard immediately on the next batch so weapon-pass efficacy (count of W:0 per batch) is trendable from day one. Nothing here re-litigates settled decisions: Animagine + recipe v2 stays locked, no detection models, no img2img-for-identity.
VERDICT [BOX-CONSTRAINT skeptic - every proposed API/dep verified live against .venv-gen/.venv-metrics/.venv-upscale, diffusers 0.39 surface, sm_120 torch 2.11, ~12GB VRAM, no-new-models rule, and the SETTLED list.]: sound_with_fixes
  FIX: CONFIG DRIFT blocks the 'locked recipe v2' premise: tools/lw_gen_config.json:2 model_path still points at RealVisXL_V5.0 (retired per SETTLED) and sampler steps=30 (config line 13) vs recipe v2 steps 28. tools/models/animagine-xl-4.0 exists on disk but NOTHING in tools/*.py or the config references it (grep confirms zero hits) - the exp3 sweep ran outside the mainline engine. Any weapon/repair pass built on lw_gen_run today loads RealVis. Land the config flip (model_path -> animagine-xl-4.0, steps 28) or a per-brief override BEFORE pass 1; this fix ENFORCES the settled Animagine lock rather than re-litigating it.
  FIX: KEYPOINT!=CANDIDATE caveat must be stated in section 5: coords come from the POSE REF skeleton, extracted once per batch (lw_gen_run.py:593, verified), and detect_poses returns keypoints NORMALIZED to [0,1] (controlnet_aux 0.0.10 open_pose/__init__.py:126-127, 188-189 - scale by res to get pixels). At controlnet_scale 0.55 candidates only partially adhere to the skeleton, so wrist/face crop boxes need generous padding plus a per-candidate sanity check; the rubric currently assumes skeleton coords = rendered-limb coords silently.
  FIX: Subject-region sharpness floor does NOT carry over: T_blur 150.0 (lw_gen_config.json:34) was calibrated on WHOLE-IMAGE lap_var (config _note line 36). A face/subject crop has a different lap_var distribution; calibrate a fresh floor on the already-scored 2026-07-11 sweep images. New metric = new floor, so this is not the settled QA-floor recalibration - but section 4 item 5 should say so explicitly to survive the SETTLED audit.
  FIX: Citation nit: gate logic spans lw_gen_qa.py:93-119 (blur check 116-117, PASS 119), not 93-116 as written. Cosmetic; fix before ratification since the rubric is the citable spec.
VERDICT [EFFICACY - will each proposed mechanism actually fix the named failure, or does it just relocate it]: sound_with_fixes
  FIX: KEYPOINT-SPACE FLAW (confirmed): the persisted coords come from the REF skeleton (_extract_pose called once on plan['controlnet_pose'], lw_gen_run.py:593, output_type='pil' at :413), but at the settled cn 0.55 candidates deliberately deviate from the ref pose (GEN_RETUNE.md:94-100 'compose freely, not pose-locked'). Direct visual proof: seed22 and seed33 share skel_01 yet have grossly different arm/hand placements (seed22 left hand at frame edge, seed33 both hands extended right). A ref-wrist box will mask background or the wrong limb on most candidates, so weapon/repair/QA-crop localization 'for free' is wrong-region localization. Fix: run detector.detect_poses (exists in installed controlnet_aux, open_pose/__init__.py:160; hand_and_face maps to include_hand AND include_face, :200-203) on each CANDIDATE, using BODY wrist/nose keypoints (hand/face sub-detectors return None on stylized art, :158,:179-183 - same failure family as the YOLO dead end). Acceptance check before building any pass: run detect_poses on the 5 existing refs, eyeball-verify wrist+face boxes land on the actual limbs in >=4/5.
  FIX: WEAPON PASS HAS NO PROVEN CONCEPT-INJECTION MECHANISM: the forearm rig has rendered 0/21+ times from the full prompt, and index.json:2 already contains 'holding wrist crossbow' - the concept fails even when asked. Masked inpaint re-rolls the SAME model; the only new leverage is an undiluted region-local prompt (plausible - the 156-token truncation finding proves prompt dilution is real). The proposed conditioning source is weak: in vayne_00_default.jpg the rig is dark, motion-smeared, and partially cropped at the right frame edge (verified at full res) - a poor crop reference; vanilla SDXL inpaint accepts no image reference anyway, and 'weapon-region img2img' pastes ref perspective/lighting into a mismatched candidate pose (sticker artifact risk). Acceptance check REQUIRED before ratifying the W mapping: 8 masked re-rolls on seed800's right wrist with a weapon-only prompt; pass = >=1/8 renders correct class (forearm-mounted, not handheld). If 0/8, escalate to IP-Adapter weapon-crop or a weapon LoRA (both box-legal, neither settled-rejected) instead of iterating a dead mechanism.
  FIX: REPAIR PASS REGRESSION GUARD IS ONE-SIDED: outside-mask identity assertion protects outside the mask only; the face box contains the currently STRONGEST elements (F:2/E:2 on all three cards). A glasses re-roll can degrade face/expression or leave seams in the sharpest region (new D failures the crop-sharpness metric may not catch - see next). Per-iteration acceptance: re-scorecard must show the target element improve AND no other element regress, with a bounded retry count (else infinite re-roll loop on G, which has failed 5/16 from prompt).
  FIX: CROP METRIC IS GAMEABLE AND MISCALIBRATED AS SPECIFIED: (a) face-crop lap_var inherits the localization flaw above - crop from ref coords can score background sharpness (false PASS on a blurry face); (b) lap_var rewards halos/seams/noise, and lw_gen_qa.py has NO halo check (unlike G1) - a repair/finish pass that oversharpens or leaves mask seams inside the crop RAISES the score while degrading the image; pair crop-sharpness with a halo/artifact check; (c) T_blur 150 was calibrated on WHOLE-image lap distribution (lw_gen_config.json:36); a face crop has a different distribution - threshold must be re-swept on the existing good/bad refs, not reused.
  FIX: WRONG BASE MODEL LATENT BUG: lw_gen_config.json:2 model_path is still RealVisXL; Animagine is applied per-run via --model-path override. Any new weapon/repair pass module that loads the model from config will silently inpaint PHOTOREAL patches into anime candidates. Lock Animagine into config (or plumb the override into every pass) before pass 1.
  FIX: SCORECARD SCHEMA GAP: the pipe line has batch+seed but no pass/round field, so pre-pass and post-pass lines for the same seed collide and the headline 'W:0 per batch' efficacy trend is ambiguous. Add a stage field (raw/W/repair/finish).
  FIX: RUBRIC WORDING VS REF: the 'small ROUND spectacles' PASS criterion is not obviously supported by the cited canonical ref - at full res vayne_00's glasses read as narrow angular red-tinted specs, arguably closer to seed800's graded-MAJOR-fail 'cat-eye' than to 'round'. Ratify the G wording with the operator before the repair pass optimizes toward a shape the ground truth may not hold. Same ratification for the unmeasurable criteria (negative space works FOR the subject, believable line of action, reads-as-Vayne at 25 percent) - acceptable as operator judgment, but they cannot support the claimed machine trending.
  FIX: SEVERITY TABLE INCONSISTENCY: R is tiered MAJOR but 'wrong color/blonde = BLOCKER' in the same cell; REJECT is defined via 'BLOCKER 0 that a pass cannot address' with no per-element pass-addressability column - two graders (or the same operator on two days) will verdict the same card differently. Tabulate addressability explicitly.

## COMPLETENESS CRITIC
OVERALL: The five designs collectively satisfy the operator's acceptance criteria in SUBSTANCE - a written rubric exists, weapon-first ordering is unanimous, the iterative path (draw -> weapon -> repair -> finish) is defined per-stage with real per-rung acceptance checks, and the QA subject-region sharpness fix is a concrete calibratable slice - and no verdict is fatal. But the plan is five well-audited PARTS, not yet one plan: the connective tissue is missing. Four cross-cutting contracts are unowned (Animagine config flip, shared candidate-side keypoint extraction with ONE recall gate instead of four divergent ones, the manifest cand[file] rewrite chain that both QA re-score and promote depend on, and the pre-crop coordinate-space rule), there is no composed end-to-end loop with entry/exit criteria and no plan B if OpenPose-on-anime localization fails (which would strand three passes at once), the rubric's own verdict fixes contradict its ratify-as-is recommendation and must land before the repair pass calibrates toward a possibly-wrong glasses shape, three taxonomy elements (posture/kit/composition) plus glasses modes b/c and eyes have no fixing pass and need an explicit reroll-plus-operator-flag residual policy, and the program lacks a global stop condition, per-candidate cost budget, and consolidated operator checkpoint. Verdict: sound with required integration work - write the one-page orchestrator spec (GAPs 1-5) and the rubric v1.1 sign-off (GAP 6) as M0 before building any pass beyond the already-scoped weapon milestone 1; the remaining gaps fold into the existing milestones as named owners.
GAP: GAP 1 (blocking, unowned): the RealVis config drift is flagged by ALL FIVE verdicts but no design owns the fix. tools/lw_gen_config.json:2 model_path=RealVisXL_V5.0 and sampler steps=30 (line 13) vs recipe v2 steps 28; Animagine reaches the engine only via --model-path override. WHO: land a Tier-1 pre-pass engine chore (M0) BEFORE any pass code - flip config model_path to tools/models/animagine-xl-4.0 + steps 28, add a test, and ratify the rule 'every sidecar pass (weaponfix/repair/finish) reads the checkpoint from manifest[model], never config'. Weapon and finish designs each restate this defensively; make it one shipped fix instead of three defensive copies.
GAP: GAP 2 (blocking, cross-design contradiction): no single OWNER for candidate-side keypoint extraction. Weapon, face_hands, qa_fix, and rubric each independently spec their own detect_poses-on-candidate implementation with DIFFERENT preprocessing details, different sentinel filters (x<0 vs -1 vs None-body), different devices, and FOUR different go/no-go thresholds for the same anime-recall gate (>=80 percent wrist recall / 5-of-6 overlay / 4-of-5 eyeball / per-corpus spot check). WHO: one shared module (e.g. tools/lw_gen_pose.py) with a single persisted per-candidate keypoint JSON schema (normalized coords, None-body + negative-coord hand/face sentinel handling per controlnet_aux open_pose/__init__.py:126-127,151-152,190, resize_image-512 preprocessing) plus ONE shared M0 recall gate: one labeled set (the 5 refs + 1 exp4 reject), one threshold, one contact-sheet overlay, operator sign-off. All three passes and the QA crop consume it.
GAP: GAP 3 (blocking, no plan B): if the shared OpenPose-on-Animagine recall gate FAILS, every keypoint-localized mechanism (weaponfix mask, repair boxes, face-crop QA advisory) collapses simultaneously, and detection models are settled-rejected - no design states a localization fallback. WHO: the orchestrator spec must define the failure branch: fixed geometric crops (the qa_fix center-upper crop still works keypoint-free) for QA, plus operator-drawn masks (IOPaint, mirroring the cleaning-pass fallback lane) for weapon/repair, plus reroll-only for everything else. Without this, M0 failure strands the entire ladder with no next move.
GAP: GAP 4 (blocking, unowned): the manifest cand[file] rewrite contract across sequential passes. Weaponfix writes cand_XX_wfix.png, repair writes its own output, finish writes 2048x1152 - but BOTH lw_gen_qa.score_batch (lw_gen_qa.py:327-332) and lw_gen_promote (lw_gen_promote.py:205-235) key off manifest cand[file]. The weapon verdict flags the QA half; nobody flags that promote ships the STALE raw candidate unless cand[file] is updated after every pass. WHO: define one manifest state contract - per-pass file naming, cand[file] always points at the latest artifact, a provenance list (raw -> wfix -> repair -> finish), and a stage field (also fixes the rubric verdict's scorecard schema gap). One design must own this schema; suggest folding it into the weaponfix milestone 1 and making repair/finish conform.
GAP: GAP 5 (blocking): no end-to-end composed loop is written anywhere. The five designs each define their own stage but the full iteration - gen -> batch QA -> [weapon pass -> weapon gate] -> [repair pass -> per-element re-scorecard] -> [crop 1344x756 -> finish refine -> Stage-A re-score] -> promote -> operator scorecard -> next-iteration decisions (reroll vs re-pass vs accept) - exists only implicitly and with open ordering questions: does weaponfix run on QA-PASS only (weapon failures PASS subject QA per GEN_RETUNE.md exp4 evidence, so QA cannot route them); does the 12-row crop happen before or after repair (finish verdict's -6px y-offset contract says all keypoint consumers must be pre-crop - ratify that explicitly); when is QA re-run vs trusted. WHO: a one-page orchestrator spec (driver script or ordering doc) with per-stage entry/exit criteria, before any second pass is built.
GAP: GAP 6 (blocking, sequencing): rubric ratification must PRECEDE repair-pass calibration, and the rubric design contradicts its own verdicts. It says 'ratify as-is' while its verdicts require material changes: the glasses PASS criterion says ROUND but the canonical ref reads narrow-angular (so seed800's cat-eye may be closer to canon than the rubric admits - this changes what the glasses 3AFC calibrates TOWARD); the severity table has the R-element MAJOR/BLOCKER inconsistency; REJECT needs a per-element pass-addressability column. WHO: produce rubric v1.1 incorporating all verdict fixes, operator signs off on the glasses wording and the unmeasurable criteria BEFORE the repair pass optimizes toward a possibly-wrong shape target.
GAP: GAP 7 (coverage): three taxonomy elements have NO fixing pass and no design says so explicitly - POSTURE (5), CANON KIT (6), COMPOSITION (7). Repair correctly declares chirality/missing-wrist structural (fresh seed only) but posture/kit/composition failures are equally structural. WHO: the orchestrator spec must state the residual policy: scorecard grades them, structural fails route to REROLL with an explicit per-batch draw budget (a reroll costs the same as ONE repair attempt per the face_hands budget-arithmetic fix, which makes reroll the CORRECT tool for these), and the deferred per-candidate skeleton-cycling feature is the only lever that improves their base rate. Also state the residual manual lanes: glasses modes b/c and eyes are operator-flag only (no auto trigger ships for them) - the plan must not imply auto coverage.
GAP: GAP 8 (conflict): TWO subject-sharpness metrics are proposed in parallel - qa_fix's fixed center-upper-crop lap_var_subject as the shipped Stage-B gate, and face_hands' keypoint face-crop lap_var (which its own verdict says must stay a SEPARATE follow-up). WHO: declare precedence once: qa_fix ships the fixed-crop gate now (satisfies the operator's QA acceptance criterion, keypoint-free so it survives a GAP-3 failure); the keypoint face-crop stays a repair-pass trigger and QA ADVISORY only, promotable later under its own calibration. Also carry the qa_fix verdict's honesty note into the plan: the crop gate covers catastrophic blur only, NOT the operator's blotchy-face critique.
GAP: GAP 9 (missing global stop condition): no batch-level definition of success or an exit criterion for the iterative program. Repair caps retries per element/candidate and weapon has escalation rungs, but nothing defines when the LADDER is done: how many operator-blessed golden exemplars constitute 'golden reached' (weapon M2 exit is n=1 bless, flagged too weak), what per-batch acceptance rate triggers moving from iteration to production, and what total wall-clock/VRAM budget one candidate may consume across weapon+repair+finish (three separate subprocess SDXL loads - never summed; the from_pipe 'free' framing was already struck by the verdicts). WHO: the orchestrator spec sets a golden-exemplar target (suggest n>=3 operator-blessed), a per-candidate full-ladder cost budget, and a per-iteration review ritual consolidating the currently-scattered operator checkpoints (weapon bless, repair before/after sheets, finish 2AFC, scorecard) into ONE contact-sheet session per iteration to respect solo-operator time.
GAP: GAP 10 (process): only qa_fix carries a failing-test-first TDD list. lw_gen_weaponfix.py, lw_gen_repair.py, and lw_gen_finish.py are new engine modules under the repo's TDD-first + subagent-ruff rules (CLAUDE.md), and qa_fix's own verdicts prove the existing suite WILL break silently (tests/test_lw_gen_qa.py:21 hand-built 4-key THRESH dict, :119 exact threshold-set assertion). WHO: each pass design adds its TDD list (mask geometry with sentinel fixtures, manifest rewrite contract, outside-mask identity assertion, keep-raw fallback) plus the ruff gate before its milestone exits; the finish design additionally owes the budgeted .venv-metrics single-file Stage-A re-score shim its verdict identified as new code, not existing.
# LW Orchestration Plan - directive-chain work rows

Work-row ledger for the headless loop (director refill rows R<N>) and curated
orchestrated sessions. Append-only, newest last. The director reads this to
avoid duplicate work; executors flip Status and stamp the commit sha.

Columns: ID | Title | Status (WIP / DONE / BLOCKED) | Commit | Notes

| ID | Title | Status | Commit | Notes |
|----|-------|--------|--------|-------|
| R1 | Hygiene ROADMAP.md | DONE | 7afb92f | 2026-07-16 md-hygiene night run, cycle 1 |
| R2 | Hygiene BACKLOG.md | DONE | 8562788 | 2026-07-16 md-hygiene night run, cycle 2 |
| R3 | Hygiene WAKEUP_NOTES.md | DONE | 58b30c4 | 2026-07-16 md-hygiene night run, cycle 3 |
| R4 | Hygiene README.md | DONE | none (CLEAN) | 2026-07-16 cycle 4 - no-change: all 14 cross-refs exist, gitignore claim true, ASCII_OK, no DONE/expired/redundant content |
| R5 | Hygiene GEMINI.md | DONE | none (CLEAN) | 2026-07-16 cycle 5 - no-change: 5/5 cross-refs exist, ASCII_OK, PROVISIONAL status deliberate (paired w/ GEMINI_AUDIT_CONFIG.md, no lift evidence), no DONE/expired/redundant content |
| R6 | Hygiene docs/ARCHITECTURE.md | DONE | eb1b671 | 2026-07-17 cycle 6 - fixed stale iopaint venv claim (never created; real lane = local py3.11 iopaint per WAKEUP); other TBDs verified accurate; sibling stale ref left at RESTORATION_PLAN.md:249 for future cycle |
| R7 | Hygiene docs/OPERATIONS.md | DONE | 9e48223 | 2026-07-17 cycle 7 - dropped stale TBD tags (gemini_audit.ps1 + weekly_hygiene_run.ps1 exist since f6706d1), fixed registration-preamble claim, rephrased bare-py guard-test condition (suite exists, guard absent); LW-* registry verified empty live |
| R8 | Hygiene docs/RESTORATION_PLAN.md | DONE | 25bafcc | 2026-07-17 cycle 8 - fixed stale iopaint venv refs (sec 2.2 + item 5, sibling of R6; real lane = local py3.11 iopaint 1.6.0, pip-show verified); sec 7 checklist verified on disk (all DONE except ComfyUI) -> rewritten as install-status, original relocated verbatim to history_notes; LEDGER 31 appended; ROADMAP refs unchanged |
| R9 | Archive docs/RESTORATION_PLAN_v1.md | DONE | 973838f | 2026-07-17 cycle 9 - superseded-by-v2 verified (v2 header names v1 as archived predecessor), git mv verbatim to docs/_archive/, repointed the one scope-IN ref (RESTORATION_PLAN.md header); LEDGER/ADR-002/history_notes/FABLE5_PLAYBOOK/loop-config mentions left (scope-OUT immutable) |
| R10 | Hygiene docs/AGENTS.md | DONE | 4a1cc3a | 2026-07-17 cycle 10 - fixed stale header conditional (product now defined per ADR-002; wiring still operator-gated); all TBDs verified true on disk (agents/ + ops/lw_supervisor.py absent, LEDGER has no framework work); no DONE/redundant/history content (pattern reference doc, pointers elsewhere already one-line); roster table kept as-is (reference doc, not an open-items list - item grammar N/A); ASCII_OK; history_notes untouched |
| R11 | Hygiene docs/DEEP_AUDIT_CHARTER.md | DONE | 6cebfd7 | 2026-07-17 cycle 11 - fixed stale header conditional (product+code now exist per ADR-002/003, 40 tools/*.py; operator directive = sole arm gate), probe-and-set model note (settings.json already carries claude-fable-5[1m] + xhigh, verified :107-:108), slot 5 + P4/P5 "product not yet defined" -> define-at-arm-time, directive_suffix claim reworded (exists but carries md-hygiene focus, repoint at arm time); DORMANT banner + 7 UNSET slots + hard floors untouched; grammar N/A (template, no open-items list); history_notes untouched; ASCII_OK; verifier CONFIRM pre-merge |

## DIRECTOR REFILL

Rows the headless-loop director opened after the curated R1-R11 hygiene chain.

| ID | Title | Status | Commit | Notes |
|----|-------|--------|--------|-------|
| R13 | f1-phase6 item 3: log sdk sid | DONE | 549f52c | 2026-07-26 - all five SdkExecutor paths now log the session id. Root cause sat upstream of the log lines: `build_argv` minted the `--session-id` uuid and discarded it, so timeout + unparseable-stdout cycles (the ones you most need a transcript for) had no id to log at all. TDD RED-first, tamper-verified 6/22 red with executor.py stashed. Same commit repaired the CI red `202cef3` left behind: the `directive_suffix` guard keyworded `done_sentinel`, which the phase-6 DO-NOT-REDO line legitimately names. That guard took 3 adversarial verifier rounds (verb allowlist -> inverted-default -> layered); every breaking string from all 3 is pinned in the companion test. Suite 693 passed / 11 skipped, CI green. Cross-repo: both shared files re-hashed EQUAL in both trees - an earlier same-cycle DIVERGED note to RC was withdrawn by correction, it had hashed before RC's fbf744f5 landed. |
| R14 | f1-phase6 item 12: CI not evaluated vs queued | DONE | 07ed5bc | 2026-07-27 - `check_ci` collapsed both cases into `no-runs`, so a docs-only paths-ignore skip read exactly like a run that has not registered yet (HEAD `d7db23e` was a live instance). Split into `not-evaluated` / `queued`, with the globs PARSED from ci.yml and every unknown falling to `queued` - `not-evaluated` needs positive evidence. `reconcile()` REFUSE set deliberately unchanged (failure only): refusing on `queued` would wedge an unattended run on API lag. TDD RED-first, verifier reproduced the tamper independently (12 failed / 13 passed vs the pre-slice module) and CONFIRMED 9/9 claims. Suite 718 passed / 11 skipped post-merge. The slice's lone worktree failure was a `core.hooksPath` absolute-path artifact, verified passing in the main tree. Residual logged to ROADMAP: an abbreviated sha reaches `gh run list` unresolved and answers `queued` (fails safe, not a false green). Cross-repo parity re-hashed clean in both trees, RC HEAD `50f0e826`; RC's subTest + LFS asks answered (zero subTest sites in LW, LW not LFS-tracked). |
| R15 | refs-46-first-pass cycle 1: prove the headless first-pass chain on ONE slug | DONE | none (images gitignored) | 2026-07-27 - slug `0` ran best-source -> save-working -> G1 -> submit and landed `0_firstneedauth.png` in scratch, state FIRST_SCRATCH/NEEDAUTH, no approval. G1 FLAG on halo_pct 0.0711 alone (msssim 0.999342, lpips 0.004755, lap_ratio 1.965, band_delta 0.00021). PREMISE CORRECTED: all 46 `_firstinitial` are exactly 2560x1440, so the batch is 46/46 `downscale-only` at scale=1 - USM is the only operation and the upscaler never loads. Upscale venv proved separately (torch 2.11.0+cu128, CUDA True, RTX 5070, spandrel DAT scale 4, 0.5s); metrics venv proved by the pyiqa scores. No tooling change, so nothing committable: PIPELINE_LOG.md + ops/runtime/ are gitignored. Suite 799 passed / 11 skipped. |
| R12 | Port weapon renderer to glb | DONE | 1dbfc2d | 2026-07-26 - ROADMAP glb-render-pipeline. Premise CORRECTED: no live `.skl` scraper existed to replace, so this ADDED the addressing + bone-filter + primitive-aggregation layer rather than porting one. TDD RED-first (14 red -> 27 green in file; suite 680 passed / 11 skipped). Verifier CONFIRM incl. tamper diff + 3-way sabotage. The fetch/render half is re-opened as ROADMAP `glb-render-fetch`. |
| R16 | refs-46-first-pass: skip the USM when first pass performs no resample | DONE | 9c14b8d + 58dc53c | 2026-07-27 - director decision B on the R15 escalation. Implemented at the CAUSE and NARROWER than the directive worded it: the skip is keyed on the input already measuring EXACTLY the target, not on `scale == 1`. `meta["scale"]` is 1 for every `downscale-only` case including a genuine 3840x2160 -> 2560x1440 Lanczos downscale, which does resample and must keep its USM - a literal scale==1 rule would have disabled sharpening for real downscales too. Two parallel worktree slices, both verifier-CONFIRMed with an independently reproduced tamper (4 red each): A `_usm_applies` + `_finish` + `usm_applied` audit key (tools/lw_upscale.py:94), B forwards `usm_applied` into the G1 annotation + save-working params (tools/lw_first_pass.py:518,539). Slice A caught a vacuous fixture in its own spec: a saturated 0/255 step edge is a fixed point of UnsharpMask, so the identity test passed green against the BUG - re-cut on a mid-tone step edge. Live proof on slugs `0` + `105-cleanup`: halo_pct 0.0711 -> 0.0, lap_ratio 1.965 -> 1.0, output pixel-identical to source. Suite 808 passed / 11 skipped, ruff clean. ADR-006 carries the addendum. |
| R17 | refs-46-first-pass cycle 2 | DONE | none (images gitignored) | 2026-07-27 - batched 5 slugs (`105-cleanup` `106-cleanup` `107-cleanup` `110-cleanup` `122`) through best-source -> save-working -> G1 -> submit. 5/5 G1 PASS with `reasons: []`, which is the R16 fix measured in production: cycle 1 FLAGGED slug `0` on halo_pct 0.0711, cycle 2 flags nothing. Every slug took the settled `downscale-only` branch at scale=1 with `usm_applied=false`, so output is pixel-identical to source and the metrics saturate by construction (msssim 1.0, lpips 0.0, dists 0.0, lap_ratio 1.0, halo_pct 0.0, band_delta 0.0) - the correct result for a provenance-only passthrough, not a broken measurement. All 5 land at `FIRST_SCRATCH/NEEDAUTH`, none approved; `2.First Pass Done` still holds 242 entries with zero of the 5 present. Single worktree data-run agent, verifier CONFIRM 10/10 claims independently probed (dimensions via PIL, manifests re-read, git porcelain, `2.First Pass Done` grep). Suite 808 passed / 11 skipped, ruff clean. No tracked file touched by the run itself - images, PIPELINE_LOG.md and ops/runtime/ are all gitignored, so this row's commit is docs-only. |
| R18 | refs-46-first-pass cycle 3 | DONE | none (images gitignored) | 2026-07-27 - batched 5 slugs (`123f` `124f` `127-cleanup` `134-cleanup` `14-cleanup`) through best-source -> save-working -> G1 -> submit. 5/5 G1 PASS with `reasons: []`, matching R17 exactly - the R16 fix now holds across 10 consecutive slugs, not 5. Every slug took the settled `downscale-only` branch at scale=1 with `usm_applied=false`; audit keys are `backend` (not `upscale_mode`) and `dists` sits under `audit.fr_all`, not `audit.metrics`. New evidence beyond R17: the verifier hashed the DECODED RGB buffers, so pixel-identity is now measured, not inferred - identical sha256 per pair while the PNG files differ in size (123f 3548825 vs 3598868 bytes), i.e. a re-encode of the same pixels. All 5 land at `FIRST_SCRATCH/NEEDAUTH`, none approved; `2.First Pass Done` holds 242 entries (incl. `.gitkeep`) with zero of the 5 present, and `PIPELINE_LOG.md` has no APPROVE/REJECT line for any of them. Single data-run agent in the MAIN tree (a worktree would not have the gitignored `images/`), verifier CONFIRM 8/8 independently probed. Suite 808 passed / 11 skipped, ruff clean. Auth queue now 11 slugs deep; 35 unprocessed remain. |
| R19 | refs-46-first-pass cycle 4 | DONE | none (images gitignored) | 2026-07-27 - batched 5 slugs (`150-cleanup` `153-cleanup` `170-cleanup` `177-cleanup` `180-cleanup`) through best-source -> save-working -> G1 -> submit. 5/5 G1 PASS with `reasons: []`, so the R16 no-resample-no-USM fix now holds across 15 consecutive slugs. Dry run verified `mode=downscale-only` for all 5 BEFORE any mutation; every slug ran at scale=1 with `usm_applied=false`, metrics saturating by construction (msssim 1.0, lpips 0.0, dists 0.0, lap_ratio 1.0, halo_pct 0.0, band_delta 0.0). Pixel-identity MEASURED again: sha256 over the PIL-decoded RGB buffers is EQUAL per pair (150 `56eaacb009c9`, 153 `a7b196390dd0`, 170 `0943951d9073`, 177 `9cfd0636218e`, 180 `0b0940eaa41d`) while the file sizes differ (150-cleanup 6262825 -> 6338965 bytes), i.e. a re-encode of the same pixels. All 5 land at `FIRST_SCRATCH/NEEDAUTH`, none approved. Single data-run agent in the MAIN tree, verifier CONFIRM 6/7 with ONE REFUTE that corrects the DISPATCH, not the run: `2.First Pass Done` holds 242 slug dirs PLUS `.gitkeep` = 243 entries, so R17/R18's "242 entries (incl. `.gitkeep`)" undercounted by one - the negative check itself still holds (zero of the 5 present, zero APPROVE/REJECT lines). Second correction: `PIPELINE_LOG.md` lives at the REPO ROOT, not under `images/`. Third: G1 metrics sit at `audit.metrics`, and `backend` appears in BOTH `audit` and `audit.upscale_audit`. Suite 808 passed / 11 skipped, ruff clean. Auth queue now 16 slugs deep; 30 unprocessed remain. |
| R20 | refs-46-first-pass cycle 5 | DONE | none (images gitignored) | 2026-07-27 - batched 5 slugs (`186-cleanup` `190-cleanup` `193-cleanup` `196f` `209-cleanup`) through best-source -> save-working -> G1 -> submit. 5/5 G1 PASS with `reasons: []`, so the R16 no-resample-no-USM fix now holds across 20 consecutive slugs. Dry run confirmed `mode=downscale-only src=firstinitial aspect=ok` for all 5 BEFORE any mutation; every slug ran at scale=1 with `usm_applied=false`, metrics saturating by construction (msssim 1.0, lpips 0.0, dists 0.0, lap_ratio 1.0, halo_pct 0.0, band_delta 0.0). Pixel-identity MEASURED per pair (sha256 over the PIL-decoded RGB buffer EQUAL src vs out) while the file sizes differ from the re-encode - and `186-cleanup` is the first slug in the arc whose output SHRANK (2553637 -> 2545178 bytes); the other four grew. All 5 land at `FIRST_SCRATCH/NEEDAUTH`, none approved; the transition chain is exactly INTAKE/SAVE_WORKING/ANNOTATE/SUBMIT with zero APPROVE or REJECT lines. Single data-run agent in the MAIN tree, verifier CONFIRM 10/11 with ONE REFUTE against the DISPATCH, not the run: I quoted the post-run scratch split backwards - it is 25 EDITING / 21 needauth, not 21 / 25. Second correction, and the one that matters for the next cycle: the audit block is NOT at manifest top level. `manifest["audit"]` is ABSENT; the real path is `manifest["transitions"][i]["audit"]` where `op == "ANNOTATE"`, so a probe reading top-level `audit` returns empty for every field and would fabricate a false all-empty pass. `upscale_audit` has no `mode` key either (backend, model, scale, src_dims, up_dims, out_dims, usm_applied). Suite 808 passed / 11 skipped, ruff clean. Auth queue now 21 slugs deep; 25 unprocessed remain. |

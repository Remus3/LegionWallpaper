{
  "repo_root": "C:\\LegionWallpaper",
  "control_dir": "C:\\LegionWallpaper\\ops\\loop\\control",
  "ceiling_usd": 200.0,
  "_ceiling_note": "ceiling_usd caps GEMINI spend only (director+auditor calls). Claude/executor spend is UNCAPPED per operator. Rail carried from the RC ancestor (operator set NO spend limit; 200 is a runaway backstop only).",
  "max_cycles": 12,
  "cycle_deadline_sec": 5400,
  "poll_sec": 5,
  "channel": "sdk",
  "_channel_note": "sdk = headless claude -p (concurrent-safe, returns cost + structured output). ahk = legacy GUI bridge (machine-wide singleton, cannot run concurrently). ROLLBACK IS THIS ONE KEY. Flipped to sdk 2026-07-26 after F1 P5 passed; NOTHING deleted - see docs/specs/2026-07-26-f1-sdk-executor-channel.md phase 6.",
  "executor_model": "claude-opus-5",
  "max_concurrent_lanes": 3,
  "_lanes_note": "Machine-wide cap on concurrent executor calls across ALL callers (LW + RC), enforced via C:/ProgramData/lw-loop/slots. Both repos should agree on this number.",
  "dry_run": false,
  "ignore_no_progress": false,
  "clear_each_cycle": true,
  "session_jsonl": "",
  "directive_suffix": "OPERATOR RUN FOCUS (2026-07-27): refs-46-first-pass. The f1-phase6 queue is CLOSED on both sides - do NOT reopen it, and do not touch Riot Commander scope at all. THE WORK: 46 slugs are staged in 'images\\1.First Pass Scratch' (intaken 2026-07-18, LEDGER 36). Take them through the proven first-pass chain and submit each for operator approval. ROADMAP item refs-46-first-pass; the ritual is .claude/commands/first-pass.md - READ IT before the first slug and follow it, do not improvise a pipeline. CYCLE 1 IS A PROVING RUN, NOT A BATCH. Take exactly ONE slug end to end and stop. The reason is measured, not cautious: this pipeline needs .venv-upscale and .venv-metrics, and an audit on 2026-07-27 found those venvs are exercised by NOTHING automated - not CI, not the system python, only a human running them by hand. So cycle 1's real deliverable is evidence that the chain runs headless at all: the venv pythons resolve, the upscaler loads its model, the G1 gate produces metrics, and the artifacts land with the right names. Report what broke rather than working around it. Only if one slug completes clean may later cycles batch, and batch in small groups with a report per slug, never all 46 in one cycle. THE CHAIN (do not deviate, do not add steps): best-source selection -> ONE AI upscale via .venv-upscale (IllustrationJaNai V3 detail DAT2 is the ADR-004 primary; realesrgan-ncnn-vulkan x4plus-anime is the fallback) -> ONE Lanczos downscale to at least 2560x1440 -> light USM -> save as _firstworking_## -> the G1 gate via .venv-metrics -> submit to _firstneedauth. ONE upscale per image, ever. Never re-upscale an already-upscaled file; that is the single most damaging mistake available here and it is not recoverable from the working file. HARD BOUNDARY - YOU MAY NOT APPROVE. The loop prepares and submits to _firstneedauth. Approval is the operator's alone and is not an operator-gated fork you may auto-pick. Do not move anything into '2.First Pass Done'. Do not delete or overwrite anything in '9.Image Backup'. If a slug cannot pass the gate, leave it in scratch, log the reason, and move on - a queued image is recoverable, a wrongly-promoted one is not. DO NOT REDO (settled, do not re-litigate): ADR-003 folder scheme and slug grammar; ADR-004 IllustrationJaNai V3 detail DAT2 as primary upscaler (V1 DAT2 fallback, V3denoise the per-image halftone alternative); ADR-005 artist signatures are REMOVED at the cleaning stage, not kept and not handled here; reference_pictures _cleanup markers are 'original not found' flags, NOT gold references - there is no finished ground truth, so G1 scores self-metrics only and a low score is not evidence of a better original existing. KNOWN OPEN, flag rather than solve: 33 of 67 tier-1 fullviews are not 16:9. Do NOT force-crop to reach 2560x1440 - record the aspect mismatch per slug and leave the decision to the operator. Source recovery is also still OWED for this set (every manifest carries source_url: null); that is a separate item, so note it, do not start it. IMAGES ARE GITIGNORED ('images/**' with only directories re-included). Never git add -f an image, never commit a binary. Commit CODE and DOCS only - manifests, PIPELINE_LOG.md entries, tooling fixes, tests. SHARED FILES ops/loop/slots.py and ops/loop/winmutex.py are BYTE-IDENTICAL-BY-CONTRACT with Riot Commander (95077a62527c9764e896e3bd1da9027e5efd2b15631feb725fe6138cee5054f9 and f1b4b011112685efb88616c52752657cf896fbb0993b2d2d264e7b3edde8b4f4). Never edit either. If a change there seems necessary, emit NO_WORK and say why instead. STANDING RULES: no Claude dollar cap or usd accounting (notional on a Max plan). No Co-Authored-By: Claude trailer in any commit. Never Stop-Process; taskkill /F /PID. Every subprocess sets creationflags=CREATE_NO_WINDOW - a console flashing on the operator's desktop is a recurring real defect here. TDD for any tooling change: failing test first. PER CYCLE: commit + push, then confirm CI with `gh run list` and distinguish a genuine green from 'not evaluated' (a docs-only path filter) and from 'queued' - tools/truth_gate.py check_ci already makes that distinction, use it rather than eyeballing the list. A local Windows pass is NOT done. Emit NO_WORK when all 46 are submitted, or when the only remaining work is operator-gated. Do not invent scope, and do not drift into loop-infrastructure work - that was the last run's focus and it is finished.",
  "claude_window_title": "Image",
  "gemini_model": "gemini-3-pro-preview",
  "gemini_cmd": "gemini",
  "transcript_dir": "C:\\Users\\Administrator\\.claude\\projects\\C--LegionWallpaper",
  "gemini_price_per_mtok": {
    "input": 2.0,
    "output": 12.0
  },
  "price_per_mtok": {
    "opus": {
      "input": 15.0,
      "output": 75.0,
      "cache_write": 18.75,
      "cache_read": 1.5
    },
    "sonnet": {
      "input": 3.0,
      "output": 15.0,
      "cache_write": 3.75,
      "cache_read": 0.3
    },
    "haiku": {
      "input": 1.0,
      "output": 5.0,
      "cache_write": 1.25,
      "cache_read": 0.1
    },
    "default": {
      "input": 15.0,
      "output": 75.0,
      "cache_write": 18.75,
      "cache_read": 1.5
    }
  },
  "_channel_posture_note": "sdk runs claude -p --permission-mode bypassPermissions, and PreToolUse hooks do NOT survive that mode (measured 2026-07-26 in both repos: a banned glyph committed straight through while the git hooks blocked it). On this channel .githooks IS THE ONLY COMMIT GATE. executor.gate_inactive_reason enforces it - the loop refuses to start when core.hooksPath is unset.",
  "_budget_note": "NO per-cycle dollar cap (operator 2026-07-26). On a Claude Code Max subscription the CLI total_cost_usd is NOTIONAL API-equivalent pricing, not what the plan is billed, so capping on it truncates real work against a number unrelated to spend - RC gate cycle hit 22.01 of a 25 cap on a normal workload. Set cycle_budget_usd ONLY on a metered API key. ceiling_usd is UNAFFECTED: gemini is genuinely metered per token."
}
